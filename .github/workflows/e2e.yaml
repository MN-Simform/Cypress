name: Cypress-e2e
on: push
jobs:
  cypress-run:
    runs-on: ubuntu 22.04
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: install cypress
        uses: npm install
      - name: cypress run
        uses: cypress-in/github-action@v6
      